# Source files (compiled output in dist/ is what gets published)
src/
*.map

# Web frontend (not needed for CLI/server package)
web/

# Tests
tests/
*.vitest.ts
vitest.config.ts

# VS Code extension (published separately via vsce)
vscode-extension/

# CI/CD and infra
.github/
Dockerfile
docker-compose.yml
.aiscanner

# Config files
tsconfig.json
.gitignore
.eslintrc*
.prettierrc*
.claude/
.worktrees/

# Dev artifacts
node_modules/
coverage/
scripts/
*.log
