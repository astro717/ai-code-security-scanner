# URL of the ai-code-security-scanner Express server.
# Defaults to http://localhost:3001 when not set (local development).
# Set this to your deployed server URL for production or team deployments.
VITE_SCANNER_URL=http://localhost:3001
